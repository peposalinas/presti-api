name: Ping Suscripciones

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl suscripciones
        run: curl -f https://presti-api-aszg.onrender.com/api/v1/suscripciones
